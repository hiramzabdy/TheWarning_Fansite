<nav class="fixed top-0 left-0 right-0 z-50 border-b border-white/5" style="background: rgba(10,10,10,0.88); backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);">
  <div class="mx-auto flex h-16 max-w-6xl items-center justify-between px-6">
    <a href="#" class="flex items-center gap-2 text-sm font-bold tracking-[0.05em] uppercase text-[#e8e0d8] no-underline">
      <span class="text-[#c62828] text-lg">⚠</span>
      <span class="hidden sm:inline">The Warning</span>
    </a>
    <ul class="hidden md:flex items-center gap-8 list-none m-0 p-0">
      {['about','members','timeline','discography','quotes'].map(s => (
        <li>
          <a
            href={`#${s}`}
            class="text-[0.8rem] font-medium tracking-[0.08em] uppercase text-[#777] no-underline transition-colors duration-300 hover:text-[#e8e0d8]"
          >
            {s}
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>
